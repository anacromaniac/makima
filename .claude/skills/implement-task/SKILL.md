---
name: implement-task
description: Implement a Makima development task. Use when asked to work on a task file from tasks/ directory, implement a feature, or when the user says "implement task" or "next task".
---

# Implement Task

Use the shared repo guidance instead of duplicating workflow rules here.

## Read first
- Read `AGENTS.md` for shared project rules and current status.
- Read `docs/task-workflow.md` for the implementation workflow.
- If the task references specific parts of `docs/makima-mvp-spec.md`, read those sections before coding.

## Claude-specific role
- Treat this skill as a trigger only.
- Follow the shared workflow in `docs/task-workflow.md`.
- Keep repo-wide instruction changes in `AGENTS.md`, not in this skill file.
