# Claude Code

Read [AGENTS.md](AGENTS.md) first. It is the shared, repo-local source of truth for project guidance.

Claude-specific note:
- If a Claude feature expects `CLAUDE.md`, treat this file as a thin entrypoint and keep shared instructions in `AGENTS.md` to avoid drift across tools.

- Project specification: see `docs/makima-mvp-spec.md` when implementation scope or feature behavior is unclear.
